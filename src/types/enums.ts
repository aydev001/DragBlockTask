export enum OrderStatus {
    NEW = "new",
    PREPARATION = "preparation",
    READY = "ready",
    ONTHEWAY = "ontheway"
}

export enum PaymentStatus {
    CASH = "cash",
    PAYME = "payme",
    CLICK = "click",
}

